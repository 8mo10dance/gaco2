version: '4'

interaction:
  npm:
    serice: client
    command: npm
    subcommands:
      run dev:
        service: client
        command: npm run dev
        compose:
          run_options: [service-ports]
  firebase:
    service: firebase
    command: firebase

provision:
  - dip npm install
